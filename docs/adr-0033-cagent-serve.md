Replace separate supervisor + Vite with cagent serve: one process. HTTP on :4242, embed.FS for UI, SSE for real-time, supervisor as goroutine. CLI proxies through service when running. See full report in work graph.
