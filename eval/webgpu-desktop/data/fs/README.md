# Workspace

This directory is the writable backend workspace exposed to the file browser app.

It ships with a few starter documents so the browser has something useful to list
the first time the desktop opens.

